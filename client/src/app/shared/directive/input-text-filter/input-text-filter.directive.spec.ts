import { InputTextFilterDirective } from './input-text-filter.directive';

describe('InputTextFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTextFilterDirective();
    expect(directive).toBeTruthy();
  });
});
